import { AdminModuleShell } from "../_components/admin-module-shell.tsx";

export default function AdminFinanceLegacyPage() {
  return (
    <AdminModuleShell
      title="Finance (Legacy)"
      description="Legacy finance route kept for compatibility while the new invoices module is used."
    />
  );
}
