import { AdminModuleShell } from "../_components/admin-module-shell.tsx";

export default function AdminBookingsPage() {
  return (
    <AdminModuleShell
      title="Bookings / Reservations"
      description="Monitor reservation flow quality, utilization, and no-show behavior globally."
    />
  );
}
