import { AdminModuleShell } from "../_components/admin-module-shell.tsx";

export default function AdminModulesPage() {
  return (
    <AdminModuleShell
      title="Modules / Features"
      description="Manage feature flags, module availability, and controlled beta access."
    />
  );
}
