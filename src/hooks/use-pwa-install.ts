/**
 * Re-exports the global PWA install context hook.
 * Use `usePwaInstall()` in any component to access the install prompt.
 */
export { usePwaInstallContext as usePwaInstall } from "@/components/providers/pwa-install-provider.tsx";
