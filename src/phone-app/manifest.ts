/**
 * Phone / store app (Capacitor ose WebView) — mbaj këtu vetëm kod që vlen për versionin mobil.
 *
 * Hyrja e shfletuesit: `phone.html` → `main.tsx` → `PhoneApp.tsx` (rrugë pa marketing site).
 *
 * Struktura:
 * - `components/` — ekrane, layout, widget-e vetëm për telefon
 * - `hooks/`      — safe area, tastierë, zbulim platforme (Capacitor, etj.)
 * - `routes/`     — harta e navigimit për shell-in mobil (opsionale)
 *
 * Import: `import { … } from "@/phone-app/components/…"`
 */
export {};
